# Copy to .env (never commit .env).
# After claiming the Bolt database these values stay the same — the
# project URL does not change. Rebuild the Electron app only if you
# migrate to a brand-new Supabase project.

VITE_SUPABASE_URL=https://qlvndvpxhqmjljjpehkn.supabase.co
VITE_SUPABASE_ANON_KEY=

# Optional: only for backup/restore and `npx supabase` CLI
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ACCESS_TOKEN=
GH_TOKEN=
